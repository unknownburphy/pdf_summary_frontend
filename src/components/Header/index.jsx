import "./Header.css";

const Header = () => {
  return (
    <div id="header-wrapper" className="flex justify-between">
      <span>some icon</span>
      <span>Hamburger menu</span>
    </div>
  );
};

export default Header;
